#include<stdio.h>
int main(){
    int n;
    double m;
    char z;
    float j;
    printf("size of in %lu bits\n",sizeof(n));
    printf("size of in %lu bits\n",sizeof(m));
    printf("size of in %lu bits\n",sizeof(z));
    printf("size of in %lu bits\n",sizeof(j));

    return 0;
}